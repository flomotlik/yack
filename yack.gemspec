Gem::Specification.new do |s|
  s.name      = 'yack'
  s.version   = '0.0.1'
  s.date      = '2012-02-17'

  s.homepage    = "https://github.com/flomotlik/yack"
  s.summary     = "Churn through Yaml file via callbacks. Define rules for specific parameters, load the yaml file and get callbacks if the routes match"
  s.description = "Churn through Yaml file via callbacks. Define rules for specific parameters, load the yaml file and get callbacks if the routes match"

  s.files = [
    'LICENSE',
    'README.md'
  ]

  s.authors = ["Florian Motlik"]
  s.email   = "flo@railsonfire.com"
end
